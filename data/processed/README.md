# Processed Data

This folder is intended for structured, cleaned, or aggregated data generated by scripts in `services/ml` from the files in `data/raw/`.

Do NOT commit large data files to git. Use this directory locally.
